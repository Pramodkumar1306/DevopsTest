trigger:
- main

pool:
  name: 'Default'

variables:
  imageName: 'simple-app'
  acrName: 'mernacr123321'
  aksCluster: 'private-aks'
  resourceGroup: 'Devops'
  namespace: 'default'

stages:

# ✅ BUILD
- stage: Build
  displayName: Build and Push Image
  jobs:
  - job: BuildJob
    steps:

    - checkout: self
      clean: true

    - task: NodeTool@0
      inputs:
        versionSpec: '18.x'

    - script: npm install
      displayName: Install Dependencies

    - task: Docker@2
      displayName: Build and Push Docker Image
      inputs:
        containerRegistry: 'ACR-Service-Connection'
        repository: '$(imageName)'
        command: 'buildAndPush'
        Dockerfile: '$(Build.SourcesDirectory)/Dockerfile'
        tags: |
          $(Build.BuildId)
          latest   # ✅ optional

    - script: |
        echo "Image pushed: $(acrName).azurecr.io/$(imageName):$(Build.BuildId)"
      displayName: Debug Image Tag


# ✅ DEV DEPLOY (AUTO)
- stage: Dev
  displayName: Deploy to Dev
  dependsOn: Build
  jobs:
  - deployment: DeployDev
    environment: dev-env
    strategy:
      runOnce:
        deploy:
          steps:

          - checkout: self

          - script: |
              echo "Connecting to AKS..."
              az login --identity

              az aks get-credentials \
                --resource-group $(resourceGroup) \
                --name $(aksCluster) \
                --overwrite-existing

              echo "Deploying DEV..."

              kubectl apply -f $(System.DefaultWorkingDirectory)/k8s.yaml -n $(namespace)

              # ✅ ONLY DEV DEPLOYMENT UPDATED
              kubectl set image deployment/simple-app-dev \
                simple-app=$(acrName).azurecr.io/$(imageName):$(Build.BuildId) \
                -n $(namespace)

              kubectl rollout status deployment simple-app-dev -n $(namespace)

            displayName: Deploy DEV


# ✅ PROD DEPLOY (MANUAL APPROVAL)
- stage: Prod
  displayName: Deploy to Prod
  dependsOn: Dev
  jobs:
  - deployment: DeployProd
    environment: prod-env
    strategy:
      runOnce:
        deploy:
          steps:

          - checkout: self

          - script: |
              echo "Connecting to AKS..."
              az login --identity

              az aks get-credentials \
                --resource-group $(resourceGroup) \
                --name $(aksCluster) \
                --overwrite-existing

              echo "Deploying PROD..."

              kubectl apply -f $(System.DefaultWorkingDirectory)/k8s.yaml -n $(namespace)

              # ✅ ONLY PROD DEPLOYMENT UPDATED
              kubectl set image deployment/simple-app-prod \
                simple-app=$(acrName).azurecr.io/$(imageName):$(Build.BuildId) \
                -n $(namespace)

              kubectl rollout status deployment simple-app-prod -n $(namespace)

            displayName: Deploy PROD